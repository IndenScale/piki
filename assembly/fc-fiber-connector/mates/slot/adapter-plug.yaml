type: slot
parent: ADAPTER-01/socket
child: PLUG-01/tip
at:
  t:
    label: "插入深度 (mm)"
    min: 0
    max: 15
    default: 0
    step: 0.5
  theta:
    label: "旋转角度 (°)"
    min: 0
    max: 30
    default: 0
    step: 0.5
  stages:
    - name: free
      required_state: null
      activates: ["t"]
      deactivates: ["theta"]
    - name: locked
      required_state: "inserted"
      activates: ["theta"]
      deactivates: []
